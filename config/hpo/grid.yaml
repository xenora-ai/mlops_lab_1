n_trials: 20
sampler: "grid"
metric: "r2"
direction: "maximize"
use_cv: false
cv_folds: 5

# Значення для перебору (сітка)
grid_space:
  n_estimators: [50, 100, 200]
  max_depth: [5, 10, 20, 30]
  min_samples_split: [2, 5, 10]